---
version: 5
defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: 'Environment data'
    paths:
      - 'nodes/%{::fqdn}.yaml'
      - 'location/%{::location}.yaml'
      - 'osfamily/%{::osfamily}.yaml'
      - 'common.yaml'
